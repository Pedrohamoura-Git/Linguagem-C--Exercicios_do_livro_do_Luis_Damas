#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/*
                            Descrição da questão

 Implemente a função abaixo que coloca a string orig imediatamente após o final da
string dest.

 *Nota:* Não colocar qualquer caractere ou espaço para separar as duas strings.

        char *strcat(char *dest, char *orig)


    (X) - Funcionando?
*/
//-------------- PROTÓTIPOS DAS FUNÇÕES ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

char *strcat(char *dest, char *orig);
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
int main()
{
    printf("Hello world!\n\n");

//------ Definindo elementos
    char *dest = "Pedro";
    char orig[] = "Moura";

//------ Armazenando o input do usuário

//------ Imprimindo o resultado
    printf("%s\n", orig[0]);
    printf("Strings concatenadas: %s\n", *strcat(dest, orig));

//------ .The End...?
    return 0;
}
//-------------- FUNÇÕES -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

char *strcat(char *dest, char *orig)
{

    int i = 0, j = 0;

    while(dest[i])
    {
        i++;
        printf("i: %d\n", i);
    }

    /*for(i, j; orig[j] != '\0'; i++, j++)
    {
        dest[i] = orig[j];
        printf("i: %d\nj: %d\n", i, j);
    }
    dest[i] = '\0';
    return dest[i];*/
}

//-------------- COMENTÀRIOS ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
/*

//(1)//
    Ex:
"Quando sair do if...incrementação seja = 0."

//(2)//
"..."

*/
