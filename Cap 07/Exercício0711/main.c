#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
/*
                            Descrição da questão

 Implemente a função abaixo que coloca um espaço em branco após cada um dos caracteres
da string 's'.

        char *strpad(char *s)

Funcionando?
    Sim - ()
    Não - (X)
----------------- COMENTÀRIOS ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//(1)//
    Por algum motivo não estou conseguindo fazer a repetição for funcionar.

//(2)//
"..."

*/
//-------------- PROTÓTIPOS DAS FUNÇÕES ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

char *strpad(char *s);
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
int main()
{
    printf("Hello world!\n\n");

//------ Definindo elementos
    char *s = "012345";

//------ Armazenando o input do usuário

//------ Imprimindo o resultado
    strpad(s);

//------ The End...?
    return 0;
}
//-------------- FUNÇÕES -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

char *strpad(char *s)
{
    int tam = strlen(s);
    char *aux = s;
    int i = 0, j = 1;
    //(1)// for(i = 1, j = 1; i <= 2*tam, j <= tam; i++)
    {
        if(i % 2 != 0)
        {
            s[i] = ' ';
            printf("s[%d] = %c\n", i, s[i]);
        }
        else
        {
            s[i] = aux[j];
            j++;
            printf("s[%d] = %c --> aux[%d]\n", i, aux[j], j);
        }
    }

    return 0;
}

